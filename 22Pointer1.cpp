#include<iostream>
using namespace std;
int main(){
    //Declaring pointers
    //Pointer is variable, and like any other variable, it must be declared before you can 
    //work with it. The asterisk(*) is used to declare a pointer.
    //Different data types have different pointers as below.
    int * x;
    float * y;
    char * z;
    double * a;
    
    return 0;
}